#!/bin/sh

# Keep main release-only. Development work belongs on development and must
# reach main through an approved pull request or merge.
branch=$(git symbolic-ref --quiet --short HEAD 2>/dev/null || true)

if [ "$branch" = "main" ]; then
  echo "Commits on main are disabled. Switch to development and commit there." >&2
  exit 1
fi

exit 0
