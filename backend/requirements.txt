# Pinned to versions verified against the current test suite (109 tests
# passing). Pydantic is on v2 — the watchlist router uses
# `ConfigDict(from_attributes=True)` which only exists in v2.

fastapi==0.141.1
uvicorn[standard]==0.22.0
pydantic==2.13.4
pydantic-settings>=2.0,<3.0
SQLAlchemy>=2.0,<3.0
python-dotenv==1.0.0
yfinance==0.2.36
httpx>=0.25
curl_cffi>=0.7

# Test-only dependencies. Kept in this file because CI installs from
# requirements.txt and the test suite needs pytest + the FastAPI
# TestClient stack.
pytest>=8.0
